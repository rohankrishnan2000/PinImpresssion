.venv/
__pycache__/
*.pyc

# Downloaded automatically on first run by the camera tracker
models/*.task

# Screenshots saved with the 'p' key
hand_*.png

# Local camera calibration (redraw if the camera moves)
tracking_boundary.json
tracking_boundary.json.tmp

# Local setup-window preferences
launcher_settings.json
launcher_settings.json.tmp
